// Default runtime config for the docker-compose deployment.
// User changes in Settings (localStorage) override this.
window.__OLLAMA_WEB_CONFIG__ = {
  ollamaHost: "/ollama",
  corsProxyUrl: "/proxy",
  ollamaDotComUrl: "https://ollama.com",
};

